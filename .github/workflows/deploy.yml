deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Install Cloudflare Tunnel Client
        run: |
          curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared.deb

      - name: Deploy to Server via SSH Tunnel
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ssh.domainin.com # 1. Adımda tanımladığın hostname
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: 3922
          proxy_command: cloudflared access ssh --hostname %h
          script: |
            docker stop pergamon-api || true
            docker rm pergamon-api || true
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/pergamon-backend:latest
            
            docker run -d \
              --name pergamon-api \
              -p 8000:8000 \
              --restart always \
              ${{ secrets.DOCKERHUB_USERNAME }}/pergamon-backend:latest
