name: Pergamon Backend CD

on:
  push:
    branches: [ "master", "main" ] # Hangi branch'e push atıldığında çalışmasını istiyorsan

jobs:
  build-and-push:
    # Docker build ve push adımların zaten burada var, oraya dokunma.
    # Eğer burası yoksa direkt altındaki deploy adımıyla devam edebilirsin.
    runs-on: ubuntu-latest
    steps:
      - name: Dummy Step (Kendi build adımlarını buraya koyabilirsin)
        run: echo "Building..."

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      # Sunucuda çalışan tünel üzerinden Docker Engine'e doğrudan TLS/HTTP komutu gönderiyoruz
      - name: Deploy to Server via Docker Context
        run: |
          echo "Connecting to application via active tunnel..."
          
          # Docker komutlarını tüneldeki web adresin üzerinden sunucuya işletiyoruz
          # Not: Sunucu tarafında Docker'ın dışarıya açık TCP portu yoksa bu adım çalışmaz.
