spring:
  datasource:
    url: jdbc:oracle:thin:@172.17.0.1:1521:FREE
    username: system
    password: quslivendus
    driver-class-name: oracle.jdbc.OracleDriver

server:
  port: 8000

springdoc:
  swagger-ui:
    path: /swagger-ui.html

axon:
  axonserver:
    enabled: false

storage:
  location: /var/www/photostore/images/  
  
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always