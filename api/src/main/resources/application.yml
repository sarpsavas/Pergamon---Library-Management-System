spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:FREE
    username: XEPDB1
    password: YeniSifren123
    driver-class-name: oracle.jdbc.OracleDriver

server:
  port: 8000

springdoc:
  swagger-ui:
    path: /swagger-ui.html

axon:
  axonserver:
    enabled: false

storage:
  location: /var/www/photostore/images/  
  
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always